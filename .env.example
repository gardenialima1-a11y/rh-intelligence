# Banco de dados PostgreSQL — aponte para o seu servidor (local, Docker, Supabase, Neon, RDS, etc.)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/rh_bi_platform?schema=public"

# NextAuth — gere um valor com: openssl rand -base64 32
AUTH_SECRET="troque-por-um-valor-gerado-com-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"
